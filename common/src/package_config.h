#ifndef PACKAGE_CONFIG_H
#define PACKAGE_CONFIG_H

// Package metadata - defaults are replaced by build scripts from package.json
// These defaults ensure the code compiles even without the build script injection

#define PACKAGE_NAME "esp32-firmware"
#define PACKAGE_DISPLAY_NAME "ESP32 Firmware"
#define PACKAGE_DISPLAY_NAME_SHORT "ESP32"

#endif // PACKAGE_CONFIG_H
